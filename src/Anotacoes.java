
public class Anotacoes {

	/*
	 * Pessoa:
	 * - String cpf;
	 * - String nome;
	 * - String email;
	 * # gets e sets pra tudo;
	 * # pessoa sera identificada pelo cpf;
	 * 
	 * Projeto:
	 * - String nome;
	 * - String objetivo;
	 * - String dataInicio;
	 * - int duracao;
	 * - String codigo;
	 * - double custoTotal;
	 * - double desConstante;
	 * - double desEventual;
	 * - double desCapital;
	 * # gets e sets pra tudo;
	 * # projeto sera identificado pelo codigo;
	 * 
	 */
}
